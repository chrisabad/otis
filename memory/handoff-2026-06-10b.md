# Handoff — 2026-06-10b (Honcho self-host Phases 1–4)

## What we did this session

### Honcho migration (AGE-627)

**Phase 1 — Docker Compose deploy ✅** (completed prior session)
- Stack at `/opt/honcho/` on VPS: api, database, deriver, redis — all healthy
- API bound to `100.117.92.5:8000` (Tailscale IP, not public)

**Phase 2 — HTTPS ✅ (pragmatic)**
- No cert: Traefik can't issue for `.ts.net`; Tailscale encrypts the transport
- Plain HTTP to `100.117.92.5:8000` is fine — Tailscale wraps it

**Phase 3 — Data migration ✅**
- Script: `/opt/honcho/migrate-from-cloud.py`
- Two bugs fixed: wrong API endpoint + wrong request schema (V3 uses `peer_id`, batch `{"messages": [...]}`)
- Result: 10 peers, 23 sessions, 5534 messages (7 truncated to 24900 chars), 0 errors
- paperclip session: 4800/4800 messages

**Phase 4 — Cutover ✅**
- All 10 `/opt/hermes-profiles/*/honcho.json` updated: `"baseUrl": "http://100.117.92.5:8000"`
- Fixed `.env` + `config.yaml` permissions: `root:paperclip 640` (hermes runs as `paperclip` user — was failing `PermissionError` after restart)
- hermes-gateway restarted and running stable

## Current state

- hermes-gateway: active (running), stable
- Honcho self-hosted: 4 containers healthy, API responding `{"status":"ok"}`
- All agents will now use self-hosted Honcho on next run

## Phase 5 — Still needed

1. **Validate** — Confirm a heartbeat run logs `base_url: http://100.117.92.5:8000` in hermes-gateway journal
2. **Cancel cloud** — Cancel Honcho cloud subscription (mcp.honcho.dev)
3. **Delete cloud key** — Remove `HONCHO_API_KEY=hch-v3-92j1...` from all agent `.env` files

## Also still open (from prior handoff)

- **AGE-784** — Run timeout watchdog (Axel in-progress)
- **Langfuse `_create_span_with_parent_context` noise** — non-fatal, low priority
- **AGE-668** — Ollama credential pooling (blocked on Hermes upgrade)
- **AGE-775** — Permission lockdown (Axel assigned)
- **Notification service (AGE-I1)** — Port 8012, needed for Juno dedup path

## Key ops notes

- SSH: `ssh -i /tmp/vps_key -o StrictHostKeyChecking=no root@100.117.92.5` (key from AWS SM `agentos/otis/vps_ssh_key`)
- Honcho health: `curl http://100.117.92.5:8000/health`
- Honcho logs: `docker compose -f /opt/honcho/docker-compose.yml logs --tail=50`
- hermes-gateway logs: `journalctl -u hermes-gateway -n 50 --no-pager`
- If hermes-gateway fails on `.env` permissions: `find /opt/hermes-profiles -name '.env' -user root -exec chown root:paperclip {} \; -exec chmod 640 {} \;`
