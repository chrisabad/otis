# Handoff — 2026-06-10 (PR cleanup + merge queue cleared)

## Current state (safe to leave)
- All AGE-5 sub-PRs (migration wave) fully merged into `paperclip-issue-trigger` main
- `tests-pr` (PR #138) fixed and merged — 212 tests, all CI green
- Docs PR #135 (AGE-762) merged
- Bot tokens auto-refreshing every 30 min; fleet fully healthy

## What we did this session

### AGE-772: Merge conflict resolution (PR #134 + PR #133)

**PR #134 — `fix/AGE-780-sdk-migration` (AGE-780)**
- 19 conflicts in `src/worker.ts`: HEAD used `sdkUpdateStatus` helper / `pfetch` wakeup;
  main (AGE-781) already had `ctx.issues.update` + `ctx.agents.invoke` equivalents.
- Resolution: take main wholesale for all 19 conflicts.
- Typecheck clean → rebuilt dist → CI green → merged ✅

**PR #133 — `fix/AGE-768-sdk-client` (AGE-768)**
- 36 conflicts in `src/worker.ts`: HEAD used `getClient().updateIssue()` / `getClient().wakeAgent()`;
  main (via AGE-525/780/781) already has the modern `ctx.issues.update` / `ctx.agents.invoke` equivalents.
- Complex: `git checkout --theirs` approach didn't work; used `git show origin/main:src/worker.ts > src/worker.ts` directly.
- Typecheck clean → rebuilt dist → CI green → ellis approval → merged ✅

### PR #138 — `tests-pr` (Add tests for predicates)

Three issues fixed:
1. `predicates.spec.ts` renamed → `predicates.test.ts` (vitest `include` only matches `*.test.ts`)
2. Merged main (which grew `a4-and-workmode.test.ts` from 1→36 tests; merge had kept wrong version)
3. Biome format fixed in both test files

Result: 212 tests (above 200 threshold). All CI green → ellis approval → merged ✅

### PR #135 — `fix/age-762-ci-guard-docs` (AGE-762 docs)

Docs-only PR. Merged main, CI green, ellis approval, merged ✅

## Current main state
- All AGE-5 pfetch→SDK migration PRs: merged (AGE-525/760/761/762/768/780/781)
- Test count: 212 (threshold: 200)
- Node.js 24 / actions v6: fully upgraded
- `src/client.ts`: present and fully functional

## Still open

### paperclip-issue-trigger
- **PR #44** — `[AGE-17634] Push unpushed commits from production tree` (May 28, Axel's work).
  Very old; likely stale. Chris should review/close if superseded.

### Infrastructure
- **AGE-784** — Run timeout watchdog: Axel investigating bypass of 1800s limit. No action needed.
- **AGE-772** (tracked as CI branch protection) — may need GitHub repo settings review for branch protection rules. The upgrades are done but review if required status checks are properly wired.
- **Langfuse `_create_span_with_parent_context` noise** — Python generator cleanup warning showing as run errors in heartbeat_runs. Non-fatal. Worth filing upstream against Langfuse 4.7.1.
- **AGE-627 / Honcho self-host** — Separate session, non-blocking.
- **AGE-668** — Ollama credential pooling blocked on Hermes upgrade.

## Key operational notes
- SSH key: fetch from AWS SM `agentos/otis/vps_ssh_key` → `/tmp/vps_key`, `chmod 600`
- DB: `docker exec -u node paperclip-ezk7-paperclip-1 psql -h /tmp -p 54329 -U paperclip -d paperclip`
- Bot token refresh log: `/var/log/refresh-gh-bot-tokens.log`
- Paperclip API: `https://paperclip-ezk7.srv1710374.hstgr.cloud/api`, auth `Bearer $PAPERCLIP_BOARD_KEY_CLOUD`
- Company UUIDs: AGE=`f4593f38`, PER=`39dc3585`, FON=`029fb83c`, KAL=`24b4c1e0`
- Ellis bot token location: VPS `/docker/paperclip-ezk7/data/.config/gh/hosts.yml`
  (auto-refreshed every 30 min by `/usr/local/bin/refresh-gh-bot-tokens.py`)
