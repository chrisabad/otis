# Handoff — 2026-06-09b (Langfuse remediation + full fleet unpause)

## Current state (safe to leave)
- All 4 companies **fully unpaused and healthy**: AGE, FON, PER, KAL
- Langfuse 4.7.1 fully operational fleet-wide — traces flowing with correct `userId=<agent_name>`
- hermes-gateway service restarted and active; all agents running on 0.16.0
- fleet-integrity-monitor.sh updated — no longer strips Langfuse every 3 minutes

## What we did this session

### 1. Root cause: fleet-integrity-monitor.sh was stripping Langfuse every 3 min
The monitor at `/opt/agentos-monitor/fleet-integrity-monitor.sh` had two rules that silently
reverted all manual Langfuse re-enablement:
- Rule 2: stripped `observability/langfuse` from every config.yaml `plugins.enabled`
- Rule 3: re-commented all `HERMES_LANGFUSE_*` env vars (adding `# integrity-off` prefix)

Ran every 3 minutes via cron. Any manual fix was reversed before the next heartbeat.

**Fix:** Removed rules 2 and 3. Backup at `.bak-langfuse-unblock-20260609`.
Rules 1 (--continue strip), 4 (corruptor), 5 (storm breaker), 6 (zombie kill) still active.

### 2. Langfuse 4.7.1 + fleet re-enablement
- Upgraded hermes (0.15.2→0.16.0) and langfuse (3.15.0→4.7.1)
- `observability/langfuse` added to `plugins.enabled` in all 17 profiles
- HERMES_LANGFUSE_* creds uncommented in all 17 `.env` files
- `export OTEL_SERVICE_NAME=<agent_name>` added to all 17 `.env` files
  (e.g. axel, juno, ellis, juno-fon, willa, piper, tess, juno-per, hollis, nell, morgan, ...)

### 3. Ollama credential pool (key-b)
- key-b (`93004d67b97047b9...`) added to all profiles as manual pool entry
- Pool: `credential_pool_strategies: {ollama-cloud: round_robin}`, 2 entries
- key-b stored in AWS SM `agentos/ollama/key-b`
- Both keys tested healthy (HTTP 200) before unpause

### 4. sessionKeyStrategy fix (Axel)
- Changed from `None` → `run` via `PATCH /api/agents/{id}/adapterConfig`
- Was resuming 139-message (~32k token) session on every run — burning quota fast
- Other agents should be audited for same issue

### 5. Langfuse userId fix (plugin patch)
**Problem:** `userId: null` in all Langfuse traces — `propagate_attributes()` never set user_id.
Langfuse dashboard couldn't group/filter traces by agent.

**Fix:** Patched `/opt/hermes-venv/lib/python3.12/site-packages/plugins/observability/langfuse/__init__.py`
- Added `user_id=os.getenv("OTEL_SERVICE_NAME") or os.getenv("HERMES_AGENT_NAME")` to
  the `propagate_attributes(...)` call in `_start_root_trace` (~line 566)
- Backup at `__init__.py.bak-userid-20260609`
- hermes-gateway restarted to activate patch
- **This is a venv-level patch** — will be lost if langfuse package is upgraded.
  Submit upstream to Hermes plugin repo.

### 6. Full fleet unpause
Step-by-step unpause with monitoring: KAL → FON → PER → AGE all healthy.
- FON: had SQL syntax error (IN() with empty agent list) — resolved via JOIN-based query rewrite
- All companies verified idle/active after unpause

## Still open
- **Submit userId fix upstream** to Hermes langfuse plugin (venv patch will be lost on upgrade)
- **AGE-784** (run timeout watchdog) — in_progress, assigned to Axel
- **AGE-771** [CRITICAL] Rotate GitHub bot tokens — still in_review  
- **Honcho "Payment required"** — Honcho migrating to self-hosted in separate session (non-blocking)
- **sessionKeyStrategy audit** — other agents beyond Axel may also have null/wrong strategy
- **`_create_span_with_parent_context` Exception ignored** in runs — non-fatal cleanup warning from
  langfuse 4.7.1, investigate if it can be suppressed

## Canary validation
- 8 consecutive `succeeded` runs for Axel post-fix
- Langfuse traces confirmed at `https://langfuse-lugt.srv1724463.hstgr.cloud` with service.name metadata
- After userId patch: traces will show `userId=axel` etc. in dashboard Users view

## Key operational notes
- SSH key: fetch from AWS SM `agentos/otis/vps_ssh_key` → `/tmp/vps_key`, `chmod 600`
- Langfuse server: `https://langfuse-lugt.srv1724463.hstgr.cloud`
- hermes-gateway service (not per-agent systemd units) is how agents run
- Paperclip API: `https://paperclip-ezk7.srv1710374.hstgr.cloud/api`, auth `Bearer $PAPERCLIP_BOARD_KEY_CLOUD`
