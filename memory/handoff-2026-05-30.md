# Handoff — 2026-05-30 (evening, interactive session with Chris)

## What happened this session

1. **`.env` created** at repo root (`/Users/cabad/Documents/GitHub/otis/.env`, mode 600) with:
   - `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`, `AWS_REGION=us-east-1`
   - `PAPERCLIP_BOARD_KEY_CLOUD` (board key for all cloud Paperclip ops)

2. **OpenClaw → Hermes cleanup** in primary context files:
   - `CLAUDE.md`: credential source path updated to `~/.hermes/workspace/agents/otis/.env`
   - `SOUL.md`: exec policy now references Hermes `safeBins` model

3. **Pulled latest** — HEARTBEAT.md updated with major stabilization context from the prior autonomous session (dispatcher removal, Axel maxConcurrentRuns 1→2, Otis offboarded from agent roster).

## Fleet state as of pull (2026-05-30 late)

- **Status: stable.** 0 failures/timeouts, ~8 issues done/90min, no DB bloat.
- Dispatcher funnel removed — AGE live, 6 other companies staged (apply on next container restart).
- Axel running at `maxConcurrentRuns=2`, no conflicts observed.
- Otis (the VPS agent record, not this CLI) terminated — was in `error`, no model.

## Open threads (carry to next session)

- **AGE-153** (Axel): patch 058 for comment checkout-auth 401 + `--resume` `Session not found` bug + code-level dispatcher removal.
- **AGE-155** (Axel): document cloud stabilization in agentos-docs (runbook + provider invariant).
- **Staged routing changes** for FON + 6 others — need a container restart to take effect; plan around FON standup.
- **Storm tripwire** does NOT auto-carry across machines — re-run checklist item 3 on any new local session.
- **FON onboarding** (AGE-2) is the next phase after Phase 2 stabilization lands.

## Terminology correction (durable)

Hermes is the primary agent harness. OpenClaw is deprecated/decommissioned. Historical references in `memory/config-changes.md` and PRDs are accurate records — don't edit them. Future guidance files use Hermes only.
