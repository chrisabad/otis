# Handoff — 2026-06-09c (DB cleanup + GitHub bot token fix + PR merges)

## Current state (safe to leave)
- Fleet fully healthy — all 4 companies unpaused, Langfuse 4.7.1 live, Hermes 0.16.0
- GitHub bot tokens auto-refreshing every 30 min via cron (no more expiry storms)
- All 5 stalled PRs from AGE-771 merged
- DB bloat cleared, disk at 54% (45 GB free)

## What we did this session

### 1. DB purge (document_revisions + activity_log)
- `document_revisions`: 303 MB → 8 MB — kept last 10 revisions per document, deleted 118,835 storm-era rows. No FK dependencies.
- `activity_log`: 127 MB → 61 MB — deleted 97,007 rows older than 7 days (nothing older than 30 days existed yet; system only started May 28).
- `heartbeat_runs`: VACUUM FULL only — 172 MB → 10 MB.
- Total freed: ~520 MB. VACUUM FULL run on all three tables.
- DB connection pattern: `docker exec -u node paperclip-ezk7-paperclip-1 psql -h /tmp -p 54329 -U paperclip -d paperclip`

### 2. GitHub bot token auto-refresh (AGE-771)
Root cause: `ghs_*` GitHub App installation tokens expire after 1h. No refresh mechanism existed.

**Fix deployed:**
- Script: `/usr/local/bin/refresh-gh-bot-tokens.py` on VPS
- Uses private keys from `~/.openclaw/credentials/github-apps/{axel,ellis}/private-key.pem`
- JWT-mints fresh tokens via GitHub API, writes to `/docker/paperclip-ezk7/data/.config/gh/hosts.yml` with correct `paperclip:paperclip` (uid/gid 1000) ownership
- Cron: `*/30 * * * *` — refreshes every 30 min, well inside the 1h expiry
- Log: `/var/log/refresh-gh-bot-tokens.log`
- Zero human intervention required going forward

### 3. PR merges (all 5 from AGE-771 blocklist)
- PR #208, #132, #28, #30 (agentos-config): already merged before session
- PR #126 (AGE-525, paperclip-issue-trigger): fixed Biome formatting (long lines → multi-line), CI went green, merged ✅

### 4. GitHub Actions Node.js 24 upgrade (paperclip-issue-trigger)
PR #139 — merged. Deadline was June 16, 2026 (7 days away).
- `actions/checkout@v4` → `v6`
- `actions/setup-node@v4` → `v6`
- `tailscale/github-action@v2` → `v4`
- `node-version: '20'` → `'24'` in all 4 validate.yml jobs
- Approved by `axel-agentos[bot]`, merged by chrisabad

## Still open

### Immediate / actionable
- **AGE-772** — Fix CI branch protection for `chrisabad/paperclip-issue-trigger`. Listed as todo. The actions upgrade is done but the underlying branch protection rules may still need review.
- **`_create_span_with_parent_context` Exception ignored** — Langfuse 4.7.1 Python generator cleanup warning showing up as run "failures" in `heartbeat_runs`. Non-fatal but pollutes failure metrics. Worth filing upstream and/or suppressing locally.

### In-progress (Axel has it)
- **AGE-784** — Run timeout watchdog: some runs bypass 1800s limit and run to 3500s+. Axel is investigating.

### Backlog / blocked
- **AGE-627 / Honcho self-host** — Replace `mcp.honcho.dev` with VPS-hosted instance (cost reduction). Separate session, non-blocking.
- **AGE-668** — Persistent Ollama credential pooling blocked on Hermes upgrade. Manual pool is the current workaround.

## Key operational notes
- SSH key: fetch from AWS SM `agentos/otis/vps_ssh_key` → `/tmp/vps_key`, `chmod 600`
- DB: `docker exec -u node paperclip-ezk7-paperclip-1 psql -h /tmp -p 54329 -U paperclip -d paperclip`
- Bot token refresh log: `/var/log/refresh-gh-bot-tokens.log`
- Paperclip API: `https://paperclip-ezk7.srv1710374.hstgr.cloud/api`, auth `Bearer $PAPERCLIP_BOARD_KEY_CLOUD`
- Company UUIDs: AGE=`f4593f38`, PER=`39dc3585`, FON=`029fb83c`, KAL=`24b4c1e0`
