---
name: project_honcho_selfhost_cutover
description: "Self-hosted Honcho — QUEUE FULLY DRAINED 2026-06-17; single canonical instance at https://honcho.srv1724463.hstgr.cloud (Langfuse VPS 2.25.162.2); deriver+embeddings on OpenAI; DERIVER_FLUSH_ENABLED=true required"
metadata: 
  node_type: memory
  type: project
  originSessionId: 0c16ce1b-8d09-4a16-a951-69efe1db8420
---

**STATUS 2026-06-11 (cutover executed):** Switched Honcho to OpenAI "for everything" (Chris's call). Real OpenAI key (`sk-proj-kONFH8…`, $20/mo cap) validated (embeddings 1536-dim OK, gpt-5.4-mini available), stored in **AWS SM `agentos/honcho/openai_api_key`** (us-east-1), written to `/opt/honcho/.env` as `LLM_OPENAI_API_KEY` (backup `/opt/honcho/.env.bak-20260611`). Set `DERIVER_ENABLED=true`; changed `DIALECTIC_LEVELS__{minimal,low}__MODEL_CONFIG__MODEL`→`gpt-5.4-mini` and commented their `OVERRIDES__BASE_URL` (was ollama.com) so dialectic+embeddings+deriver all hit OpenAI. `docker compose up -d` — api healthcheck times out at 5s on slow boot (false "unhealthy"; recovers ~2min then truly healthy). GOTCHA: deriver (`honcho-deriver-1`, separate service, entrypoint `python -m src.deriver`, `depends_on api:service_healthy`) got stuck in "Created" because api was transiently unhealthy at boot → fix with `docker compose up -d deriver` once api healthy. Re-enqueued 964 old Ollama-AuthError units: `UPDATE queue SET processed=false,error=NULL WHERE error IS NOT NULL`. Pipeline VERIFIED: embeddings succeed (`used_embedding` in api logs), deriver completes units (8–44s each), **0 OpenAI errors**. Chris's representation session processed first. **UPDATE 2026-06-12: the $20/mo OpenAI cap was EXHAUSTED in ~16h after ~2560 units** (representation-building on full meeting transcripts is far more token-heavy than estimated) → every unit then failed `429 insufficient_quota`, retried 3×, spiked box load to ~40. **DERIVER STOPPED** (`cd /opt/honcho && docker compose stop deriver`) to halt the thrash. State: ~2560 derived (incl. chris ~66%+), ~5400 pending, ~414 quota-failed (auto-retry on resume). TO RESUME: raise OpenAI cap (~$40–60 for full drain) → `docker compose start deriver`. CAVEAT: 2-core/8GB box undersized — drain is slow (~2–8/min) and runs the box hot (load also affects paperclip + agent gateways). DERIVER_WORKERS defaults to 1; DON'T raise (postgres/pgvector is the bottleneck, box already hot). Deriver tuning knobs: `/opt/honcho/.env.template` lines 99–131. WATCH the $20/mo OpenAI cap on this initial drain. STILL PENDING: (1) MCP cleanup — remove cloud wrapper (`.claude/honcho-mcp.sh`→mcp.honcho.dev) + `honcho@honcho` plugin so nothing bills cloud; use /v3 REST until a v3-capable JS MCP ships. (2) Rotate OpenAI key (pasted in chat). (3) Final search/chat verification once drain completes. Queue/status: `GET /v3/workspaces/Agentos/queue/status`. DB queue table cols: processed(bool), error, task_type(representation/summary/reconciler), session_id, work_unit_key.

---

**UPDATE 2026-06-16 (fully operational):** Deriver back online with real OpenAI key (new key generated 2026-06-16, stored in AWS SM `agentos/honcho/openai_api_key`). Final model config on BOTH Honcho instances (100.117.92.5 AND 2.25.162.2):
- **Embeddings**: real OpenAI `LLM_OPENAI_API_KEY` → `text-embedding-3-small` (default, no override needed)
- **Deriver**: real OpenAI `gpt-5.4-mini` (default, no model override — `DERIVER_MODEL_CONFIG__*` not set) — ~1.5s/unit, reliable Pydantic structured output
- **Dialectic (minimal + low)**: LiteLLM `lightweight` (ministral-3:3b) via `DIALECTIC_LEVELS__*__MODEL_CONFIG__OVERRIDES__*` — free, plain text gen only
- deepseek-v4-flash (via LiteLLM `routine` alias) CANNOT do Honcho deriver structured output — returns empty JSON ("No parsed content in structured response")
- `docker compose restart` does NOT reload env_file; must do `docker compose stop deriver && docker compose up -d` to apply new env vars
- TWO independent Honcho instances exist: 100.117.92.5:8000 (fleet agents connect here, port 0.0.0.0:8000 bound) and 2.25.162.2 (behind Traefik, `https://honcho.srv1724463.hstgr.cloud`, my Claude Code session uses this one). Both have same data from prior migration dump; both processing independently.

---

**QUEUE FULLY DRAINED 2026-06-17:** 7,781/7,781 Agentos work units completed. Root cause of "129 tasks stuck": `DERIVER_REPRESENTATION_BATCH_MAX_TOKENS=1024` (default) — deriver silently skips representation tasks with fewer than 1024 accumulated tokens, so short-message sessions never get processed. Fix: `DERIVER_FLUSH_ENABLED=true` in `/opt/honcho/.env` bypasses the threshold. This is now set permanently and backed up to AWS SM `agentos/honcho/env`. GOTCHA: active long sessions (this Claude Code session) keep generating new `summary_claude_code` queue items that burn OpenAI quota before the deriver gets to the Agentos tasks — workaround: `UPDATE queue SET processed=true WHERE workspace_name='claude_code' AND processed=false;` to skip them. Total OpenAI spend for full drain: ~$60 ($20 × 3 top-ups). Deriver now at ~1.5s/unit (gpt-5.4-mini, 6 observations/unit avg).

---

**CONSOLIDATION 2026-06-16:** Two independent Honcho instances (one on each VPS) were running because fleet agent `.env` files pointed to `100.117.92.5:8000` after migration, so that instance was kept alive. Fixed: dumped 100.117.92.5 DB, restored to 2.25.162.2, updated all 10 fleet agent `.env` files to `HERMES_HONCHO_HOST=https://honcho.srv1724463.hstgr.cloud`, ran `docker compose down -v` on 100.117.92.5 and deleted `/opt/honcho/`. Also updated `~/.honcho/config.json` on local Mac to point to canonical URL. Full `/opt/honcho/.env` backed up to AWS SM `agentos/honcho/env`. Key durability facts:
- `docker compose restart` does NOT reload `env_file` — must `docker compose stop X && docker compose up -d` 
- Port 8000 is NOT host-bound on 2.25.162.2 (behind Traefik only); access via `https://honcho.srv1724463.hstgr.cloud`
- `docker compose down -v` removes volumes; `docker compose down` alone does NOT

**MIGRATION 2026-06-14:** Honcho moved from main VPS (100.117.92.5) to Langfuse VPS (2.25.162.2, srv1724463). Now at `https://honcho.srv1724463.hstgr.cloud` (behind Traefik, HTTPS, Let's Encrypt cert). Stack at `/opt/honcho/` on Langfuse VPS — same docker-compose, ports now internal only (`expose: 8000`, no host bind). DB restored from 194MB pg_dump (5 workspaces, 32 sessions, 41k messages, 500 embeddings). All 10 agent `honcho.json` files on main VPS updated: `baseUrl` → `https://honcho.srv1724463.hstgr.cloud`. SSH key for Langfuse VPS added to Hostinger panel (same key as main VPS: `agentos/otis/vps_ssh_key`). Honcho postgres on Langfuse VPS exposed at `127.0.0.1:5433` (offset from Langfuse's postgres on 5432); redis at `127.0.0.1:6380`. Reason for move: Honcho deriver was a major CPU load contributor on the already-saturated main VPS (load 10-19); Langfuse VPS was near-idle (load 0.17). Post-move main VPS load trending from 19 → 11.

Self-hosted Honcho v3 runs on Langfuse VPS — containers `honcho-api-1`, `honcho-database-1` (pgvector pg15), `honcho-redis-1`, `honcho-deriver-1`. ORIGINAL DIAGNOSIS (2026-06-10, why cutover wasn't done) — four blockers, now resolved by the status block above:

1. **MCP config never repointed.** Commit 6c92bca's message claims it pointed Honcho at self-hosted "via local wrapper script," but `.claude/honcho-mcp.sh` still execs `npx mcp-remote https://mcp.honcho.dev` (CLOUD, billed). All `mcp__honcho__*` calls hit cloud → `chat` 402s "insufficient credits." There are also TWO competing honcho MCP registrations: repo `.mcp.json` (the wrapper) AND the `honcho@honcho` plugin (`~/.claude/plugins/.../honcho/0.2.5`, SDK-based, defaults to api.honcho.dev). Both cloud. Consolidate to one.

2. **JS SDK/MCP version gap.** Self-hosted exposes ONLY `/v3/` API (openapi confirms; `/v2/*` → 404). But the published `@honcho-ai/sdk` (latest 2.1.2), `@honcho-ai/core` (2.2.0), and official `@honcho-ai/mcp` (2.2.0) all build `/v2/workspaces` paths. So NO published JS MCP/SDK can talk to the v3 box today. Options: access self-hosted via `/v3` REST directly (works now — same pattern as plain-support skill's shell scripts), wait for a v3 JS SDK, or write a thin v3-REST MCP shim. REST `/v3` works today: list/search/messages endpoints respond; e.g. `historical-meetings-openclaw` session has 3400 migrated messages.

3. **Deriver is OFF.** `DERIVER_ENABLED=false` on honcho-api. Queue has ~6537 pending work units (965 completed). Deriver builds the embeddings/representations that power `search` + `chat`; with it off, migrated messages sit unprocessed → `search` returns 0, `chat`/dialectic empty. Likely disabled to stop hammering the rate-limited LLM (see #4).

4. **No working LLM/embedding provider on the box.** Dialectic routed to `https://ollama.com/v1` model `glm-5.1:cloud` (shared Ollama Cloud → 429 RateLimitError storm, ~11/500 log lines; this is the documented [[incident_age_langfuse_crash_and_ollama_quota]] shared-kaleidoscope quota issue). `LLM_OPENAI_API_KEY` is NOT real OpenAI (prefix `9e1dc95...`, 57 chars, not `sk-`; returns 401 on api.openai.com) — it's an Ollama key in OpenAI-compat mode. So embeddings (default text-embedding-3-small) AND generation both depend on the throttled Ollama account. Fixing search+chat requires funding a non-rate-limited provider (real paid OpenAI key, dedicated/paid Ollama tier, or local models on VPS) — a billing decision, same one flagged in the Ollama quota incident.

**CRITICAL (2026-06-10 update): chrisabad Ollama account CANNOT run a functional Honcho.** Honcho config is at `/opt/honcho/.env` (compose `/opt/honcho/docker-compose.yml`). Fleet's healthy key = `OLLAMA_API_KEY=93004d67b97047b9...` (in all /opt/hermes-profiles/*/.env; chat glm-5.1:cloud → HTTP 200). Honcho was on a DIFFERENT/stale key `9e1dc95...`. BUT swapping the key doesn't fix it because Ollama Cloud lacks two capabilities Honcho's core needs: (1) **structured output** — tested glm-5.1:cloud with strict json_schema → ignored schema, returned markdown; json_object → fenced ```json. Deriver requires schema-conformant JSON, so `DERIVER_ENABLED=false` (config comment: "Ollama Cloud doesn't support structured output (required by deriver)"). No deriver → no peer representations → `chat`/dialectic returns nothing useful even at HTTP 200. (2) **embeddings** — `/v1/embeddings` 404 for all models on ollama.com → `search` can't work. VPS too small for local generation (2 cores/7.8GB RAM/no GPU); a local CPU embedder (nomic-embed-text, 768-dim, set EMBEDDING_VECTOR_DIMENSIONS=768; VECTOR_STORE_MIGRATED=false so rebuild expected) is the only no-new-billing path for embeddings, but deriver generation still needs a structured-output API. **To finish, need an OpenAI-compatible provider (real OpenAI gpt-5.4-mini + text-embedding-3-small is cheapest/simplest — pennies) for at least deriver + embeddings; dialectic chat can optionally stay on chrisabad Ollama.** Decision pending with Chris.

**Remaining cutover checklist:** (a) pick + fund a working LLM/embedding provider; (b) set it as Honcho's LLM + embedding config (real `LLM_OPENAI_API_KEY=sk-...` or repoint dialectic/deriver model configs); (c) `DERIVER_ENABLED=true` + restart honcho-api, let it drain the 6537-unit backlog; (d) verify `/v3 .../search` returns results and dialectic chat works; (e) decide MCP strategy — remove the cloud-pointed wrapper + plugin to kill confusion, use `/v3` REST directly until a v3-capable JS MCP exists. Honcho self-hosting docs: https://honcho.dev/docs/v3/contributing/self-hosting (LLM_OPENAI_API_KEY required or server fails to start; deriver is a separate essential worker; clients connect via base_url param).
