# Handoff — 2026-06-08 (fleet self-sabotage incident + usage investigation)

## Current state (safe to leave; self-healing)
- Fleet **stable, no active burn** (0 runs/0 failed in the quiet window at close).
- All known regressions reverted + a **fleet-integrity-monitor** cron auto-heals drift every 3 min.
- **AGE work is PARKED**: ~30 AGE issues unassigned→backlog (recoverable via re-triage, not lost). Juno AGE unpaused but idle.
- **FON + PER**: healthy-capable, idle. PER (Personal, ops-only company `39dc3585`) was bootstrapped + validated earlier this session (Juno/Hollis/Nell/Morgan).

## What happened (root cause)
The autonomous fleet **kept undoing prior incident fixes via its own tickets**, taking the whole fleet down repeatedly over ~12h:
1. **AGE-694** (Axel): wrote/ran `ensure-hermes-continue-flag.sh`, injecting `--continue` into all 14 `/opt/hermes-wrappers/*.sh`. `hermes --continue chat …` makes `--continue` swallow the `chat` subcommand → **exit 2, ~1s, before any model call** → every agent crash-looped → recovery storm (18k+ failed runs/day). Re-ran at 06:03 after my first fix. PR #195 (the landmine — would re-corrupt on every deploy) **closed**.
2. **AGE-635** (Axel, with AGE-633/634): re-enabled Langfuse fleet-wide ("pinned langfuse 4.7.1 + otel 1.42.1"). Still **crashes runs** (TypeError in `_create_span_with_parent_context`) — the pin did NOT fix the live venv. This was the original incident's cause, back. Reverted via **PR #196**.
3. Recovery storm amplified both (breaker patch 059 present but ineffective; recovery ignores agent-pause).

## Usage finding (Chris's question)
- The scary churn (exit-2 crashes, 18k/day) cost **~0 Ollama tokens** (crash before the model call). It was churn, not burn.
- The actual token burn = runs that **executed**: real agentic runs (each = many large-context LLM calls) + the Langfuse-crash runs that ran-then-crashed. Per-run cost is high → **run volume is the efficiency lever**.
- 429s are rejected requests → consume nothing. Prior weekly spikes were storm days (Jun 1/6/7) aging through the rolling window.

## Durable fixes applied this session
1. **`/opt/agentos-monitor/fleet-integrity-monitor.sh`** (cron `*/3 * * * *`, log `/var/log/fleet-integrity-monitor.log`): every 3 min strips `--continue` from wrappers, removes `observability/langfuse` from config.yaml, comments `HERMES_LANGFUSE_*` creds, disables any `ensure-hermes-continue-flag.sh`, and **caps recovery storms** (if active recovery actions > 20 → resolve + unassign/park source issues).
2. **agentos-config PR #196 merged**: langfuse disabled in all 16 repo config.yaml (reverts AGE-635) so deploys don't re-enable.
3. Live: langfuse off (config + creds), all 14 wrappers reverted to `exec hermes "$@"`, corruptor script disabled, PR #195 closed.

## NEXT STEPS (do these in a fresh session — this one hit ~10× normal token cost)
1. **AGE-702 (CRITICAL guardrail)** — the monitor only auto-CORRECTS; build PREVENTION: agents must not write `/opt/hermes-wrappers`, `config.yaml`, `.env`, or the deploy workflow. Permission lockdown (check agent run user) + deploy-workflow approval gate. *An agent took down the whole fleet — this is the headline fix.*
2. **AGE-623** — Paperclip recovery must respect agent pause (dispatch-failure-monitor pauses on storms, but recovery/service.js ignores it + re-dispatches). Patch + container restart. Monitor storm-cap is the interim.
3. **AGE-624** — Langfuse stays OFF until the venv crash is reproduced-fixed + verified fail-open. AGE-633/635 did not work.
4. **Re-triage the ~30 parked AGE issues** to resume AGE work (they're backlog/unassigned).
5. **AGE-668** — real Ollama credential pooling needs a Hermes upgrade (v0.15.2 prunes manual pool creds); revisit when a 2nd durable account exists. All fleet agents currently on the single kaleidoscope key.
6. Rotate exposed secrets if not already (Juno pcp key surfaced in an earlier session transcript; langfuse pk/sk; etc.).

## How to verify / operate
- Detailed forensics in `.claude` auto-memory: `incident_fleet_wrapper_corruption_2026-06-08.md`, `project_per_bootstrap.md`, `hermes_ollama_key_pooling_limitation.md`, `incident_age_langfuse_crash_and_ollama_quota.md`.
- VPS (local session, Tailscale): SSH key from AWS SM `agentos/otis/vps_ssh_key`; `root@100.117.92.5`.
- Run state ground truth: embedded Postgres in `paperclip-ezk7-paperclip-1`, socket `/tmp` port 54329, db/role `paperclip`. Table `heartbeat_runs` (status/error_code/exit_code/stdout_excerpt/stderr_excerpt). Ollama per-run tokens NOT recorded (usage_json null) — use Ollama dashboard for $.
- Quick health: `select status,count(*) from heartbeat_runs where started_at>now()-interval '15 min' group by 1;`
- Integrity monitor working: `tail /var/log/fleet-integrity-monitor.log` (logs only when it corrects drift).

## The meta-lesson
Autonomous agents are powerful enough to break their own infrastructure, and they will re-attempt "improvements" (Langfuse re-enable, session-continuity) that undo fixes. Until AGE-702 guardrails exist, the integrity monitor is the safety net. Also: keep Otis sessions SHORT/scoped — this mega-session cost ~10× normal (long context re-read every turn).
